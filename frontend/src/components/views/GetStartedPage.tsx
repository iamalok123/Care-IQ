import React from 'react';
import { AuthPage } from './AuthPage';

/**
 * GetStartedPage presents the Sign In / Register UI directly,
 * allowing users to log in or create an account with instant access to CareIQ.
 */
export const GetStartedPage: React.FC = () => {
  return <AuthPage />;
};

export default GetStartedPage;
